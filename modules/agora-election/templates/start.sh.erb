#!/bin/bash

HOME=/home/aelection
source $HOME/.profile

# workon agora-election
source $HOME/.virtualenvs/agora-election/bin/activate
cd $HOME/agora-election/agora_election
ps aux | grep "uwsgi --ini /home/aelection/uwsgi.ini" | awk '{print $2}' | xargs kill -9
uwsgi --ini $HOME/uwsgi.ini