#!/bin/bash

if [ "$(whoami)" != "root" ]
then
    echo "You need to run this as root"
fi

AGORA_DIR=/home/agora
VENV_DIR=$AGORA_DIR/.virtualenvs/agora-ciudadana/
source $VENV_DIR/bin/activate

export PYTHONPATH=$AGORA_DIR/agora-ciudadana
/usr/bin/eopeers.py $*